spring:
  application:
    name: be-my-wealth-portfolio
  datasource:
    url: jdbc:postgresql://localhost:5430/mydatabase
    username: admin
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect : org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8081
  servlet:
    context-path: /wealth